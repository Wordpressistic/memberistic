<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnWarning="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         displayDetailsOnTestsThatTriggerNotices="true"
         displayDetailsOnTestsThatTriggerDeprecations="true">
  <testsuites>
    <testsuite name="unit">
      <directory>tests/unit</directory>
    </testsuite>
  </testsuites>
  <!--
    Coverage describes the plugin, not the one file that happened to be tested
    first. The unit suite only exercises a fraction of this, and that is the
    point: a coverage report scoped to the single file it already covers
    reports 100% forever and tells nobody anything.

    templates/ and assets/ stay out because they are output, not logic. Every
    PHP file that decides something lives under includes/, plus the bootstrap
    and the uninstaller.
  -->
  <source>
    <include>
      <directory suffix=".php">includes</directory>
      <file>memberistic-membership-solutions.php</file>
      <file>uninstall.php</file>
    </include>
  </source>
</phpunit>
