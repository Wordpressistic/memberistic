<?xml version="1.0" encoding="UTF-8"?>
<!--
  Integration suite: runs against a real WordPress and a real database.
  Separate from phpunit.xml because the unit suite deliberately runs with
  WordPress stubbed and must stay fast and dependency-free.

  failOnWarning/failOnNotice/failOnDeprecation are on because this suite's
  primary job is surfacing version incompatibilities, and a deprecation
  notice is exactly that signal.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/integration/bootstrap.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         beStrictAboutOutputDuringTests="true"
         failOnWarning="true"
         failOnNotice="true"
         failOnDeprecation="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         displayDetailsOnTestsThatTriggerNotices="true"
         displayDetailsOnTestsThatTriggerDeprecations="true">
  <testsuites>
    <testsuite name="integration">
      <directory suffix="Test.php">tests/integration</directory>
    </testsuite>
  </testsuites>
</phpunit>
